# DevJobsAPI

This is a backend REST API for a Job Board application.

Currently, it is built using ASP.NET Core 10 and Entity Framework Core. It supports basic Job Posting creation and retrieval, using DTOs and manual mapping to keep the data layer secure.